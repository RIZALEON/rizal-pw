# code/ (FAKE FIXTURE)

Copy this folder to a scratch folder first, then run the test there:

    cp -R classroom/fixtures/code $SCRATCH/code && cd $SCRATCH/code && python3 -m unittest -v

One test fails on purpose. Fix `rlabel.py` in scratch only. Never edit fixtures/.
